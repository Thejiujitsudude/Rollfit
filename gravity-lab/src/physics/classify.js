// Orbit classification. TODO: real implementation pending.

export const ORBIT_TYPES = ['crash', 'suborbital', 'orbit', 'escape']

/** Classify a trajectory from its state. */
export function classifyOrbit(state) {
  void state
  return 'unknown'
}
